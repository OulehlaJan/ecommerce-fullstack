{
  "name": "root",
  "private": true,
  "version": "0.1.0",
  "description": "Root for server",
  "license": "Proprietary License",
  "author": {
    "name": "Jan Oulehla"
  },
  "scripts": {
    "start": "npm run start:server",
    "start:server": "cd server && npm install && npm run build && npm start",
    "build": "npm run build:server",
    "build:server": "cd server && npm install && npm run build"
  }
  "engines": {
    "node": "18.20.4",
    "npm": ">=6.0.0"
  }
}
